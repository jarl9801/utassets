import { render } from '@testing-library/react';
import React from 'react';

// Basic sanity test to ensure testing library is set up correctly
it('renders without crashing', () => {
  render(<div />);
});
